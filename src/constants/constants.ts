export enum Routes {
    LOGIN = "/login",
    REGISTER = "/register",
    FORGET_PASSWORD = "/forgetpass",
    RESET_PASSWORD = "/resetpass",
    CHANGE_PASSWORD = "/changepass",
}

export enum ROLES {
    INSTRUCTOR = "Instructor",
    STUDENT = "Student",
}

export enum INSTRUCTOR_ROUTES {
    DASHBOARD = "/dashboard",
}

export enum STUDENT_ROUTES {
    HOME = "/home",
}